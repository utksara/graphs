
#include<iostream>
#include"airport.h"
#include "stdafx.h"

/*statetransition(aircraft a1) {

	if (a1.get_position.isvacant()) {
		a1.position.reset();
		a1.goto_next_state();
	}

	else {
		proceed(as.state.time);
		if (as.state.time < 0) {
			a1.waitime++;
		}
	}
}*/